const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`the sum is : ${num1 + num2}`);
}

function multiplyValues() {
  console.log(`the multiplication of numbers : ${num1 * num2}`);
}

addValues();
multiplyValues();
